import styled from 'styled-components'

const color = `var(--theme-color-foreground-primary)`

const Copy = styled.p`
  color: ${color};
  line-height: 1.5;
  max-width: 700px;
`

export default Copy
