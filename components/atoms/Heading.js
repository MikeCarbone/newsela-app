import styled from 'styled-components'

const color = `var(--theme-color-foreground-primary)`
const fontSize = `var(--theme-fontSize-4x)`

const Heading = styled.h1`
	color: ${color};
	font-size: ${fontSize};
`

export default Heading
